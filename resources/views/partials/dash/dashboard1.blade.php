<section class="container">
    <h1 class="text-center bg-dark text-white">2 team fulls</h1>
    {{-- @foreach ($teams->where('max', "=", ) as $item) --}}
        <p>Conditions pas encore trouvées.. </p>
    {{-- @endforeach --}}
    {{-- @foreach ($players ->where("team_id", "!=", 1) as $item)

    <p>{{$item->name}}</p>
        
    @endforeach --}}
</section>